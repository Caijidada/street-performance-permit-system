# 街头演出点位报备系统 — 前端

基于 Vue 3 + Vite + Element Plus 构建的前端工程。

## 启动

```bash
npm install
npm run dev      # 开发模式（localhost:5173）
npm run build    # 生产构建
```

## 环境变量

在根目录创建 `.env` 文件：

```env
VITE_AMAP_KEY=你的高德地图JS_API_Key
```

详细说明见项目根目录 `README.md`。
